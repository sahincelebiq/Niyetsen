---
description: Niyetsen proje hafızası — sözlük, kilitli kararlar, tuzaklar, dosya haritası (her oturumda geçerli)
alwaysApply: true
---

# NİYETSEN HAFIZASI (proje beyni — v2, 2026-08-02)

Bu dosya projenin damıtılmış kurumsal hafızasıdır. Bir karar buradakiyle
çelişiyorsa ÖNCE burada/MASTER_PLAN'da neden öyle olduğuna bak — çoğu madde
canlıda yaşanmış bir hatanın veya Şahin'in verdiği bir kararın izidir.

> **Play Store çıkış sprinti aktif** → ayrıntılı açık/kapalı liste:
> `.cursor/rules/playstore-cikis-hafiza.mdc` (alwaysApply).

## Sözlük (Şahin'in dili → sistem karşılığı)

- **Zincir** = streak. Gün sınırı kullanıcı timezone 23:59. "Zincir Koruma
  Jetonu" = ayda 1 otomatik freeze token.
- **Mazeret** = missed_excused (−25 sabit, katlanmaz). **Sessiz kaçırma** =
  missed_silent (−25×2ⁿ, tavan 200). Puan tabanı 0 — negatif yok.
- **Mistik / gizli bölüm** = fal modülü (tarot/kahve/el/burç), MysticColors
  teması, ☾ butonu. Fal = AYNA, kader değil; her yanıtta disclaimer.
- **İdol Modu / Felsefe Yolları** = kamuya açık kişilerden İLHAM alan
  felsefe-adlı paketler. YASAL KURAL: paket adı felsefe adıdır, kişi adı yalnız
  "…ilham alır; kendisiyle bağlantılı değildir" notunda geçer. Premium (dev muaf).
- **Dev hesabı** = kutluadalarr7@gmail.com (DEV_ACCOUNT_EMAILS) — abonelik
  kısa devresi, mağaza satın alması olmadan tam erişim.
- **KAPI** = faz çıkış kriteri. Geçmeden sonraki göreve BAŞLANMAZ.
- **Wrapped / Rapor** = /me/recap story özeti. Yalnız kazanımlar gösterilir.
- **İlkbahar** = UI v3 paleti (genç yaprak #35814A + mercan #E06842).
  Sonbahar/pastel geri gelmez. Soft dark = simsiyah değil (`theme.ts` dark).
- **Minimal ölçek (v3.1, 2026-08-05, KİLİTLİ)**: title 32 / screenTitle 22 /
  subtitle 18 (`themed-text.tsx`) — büyütme; vurgu boşluk/renkle.
- **"KAPI İÇERİDE"** (Şahin kuralı): PRO bölümler (rapor/İdol) ücretsiz
  kullanıcıya GÖRÜNÜR — dışarı atma yok, içeride kilitli önizleme + "PRO ile
  aç" CTA (örnek: rapor.tsx). Fal ise tamamen ücretsiz (hak sayaçları
  sunucuda). Detay: `.cursor/skills/niyetsen-tasarim/SKILL.md`.
- **Ücretsiz = 1 plan**; 2. plan = abone/dev. Rank/puan kullanıcı bazlı
 (plan değişince score sıfırlanmaz).
- **2026-08-10: leaderboard öne çekildi (Şahin)** — opt-in TAKMA ADLI haftalık
 gelişim ligi (KVKK: yalnız rumuz + puan; gerçek isim/e-posta sızmaz).
 Profilde "Arkadaşlar" girişi. Utandırma yasağı ligde de geçerli.
- **2026-08-10: Mistik yeni evi = Bugün sekmesi** (ayarlar girişi değil).
 Mistik merkez = rehberle SOHBET; tarot/kahve/el kısayolları sohbete sonuç
 düşürür; fortune_log geçmişi rehber bağlamına girer (mistik hafıza).
- **2026-08-10: Zincir evrimi** — 12 hayvanlı set (bebek→ergen→yetişkin),
 zincir gün eşikleriyle büyür; emoji değil tutarlı ikon seti.

## Kilitli kararlar (tartışılmaz, tarihiyle)

- Görev +50; 6 kategori sabit: İrade, İstikrar, Disiplin, Özgüven, Sosyallik, Özsaygı.
- Ücretsiz sohbet SINIRSIZ (FAZ 7.6); plan/kanıt/bonus/İdol premium; 7 gün deneme.
- Ödeme YALNIZ IAP/RevenueCat — harici ödeme linki Apple reddi sebebi.
- Model: sohbet+plan `gemini-3.1-pro-preview` (2026-07-29); fallback 2.5 ailesi
  otomatik. Model adı UYDURULMAZ — ai.google.dev/gemini-api/docs/models.
- Kanıt: Vision güven ≥60 onay; <60 nazik tekrar (maks 3, 3.'de beyanla kabul).
  Anlamsal eşleşme ZORUNLU — aynı tema yetmez (su ≠ meyve tarifi, ≤40).
- Ton: dürüst yüzleşme ✅ ("Disiplinin düşüyor, neden vazgeçiyorsun?"),
  utandırma/aşağılama ❌. Wrapped'ta kaçırılanlar HİÇ gösterilmez.
- Cinsiyet: isteğe bağlı, yalnız hitap/örnek uyarlaması; klişe YASAK.
  String'ler birebir: "kadın" / "erkek" / "belirtmek istemiyorum".
- v1'de YOK: fine-tuning, leaderboard (v3), Pinterest (v3), tanımlı araç
  dışı function calling (core/tools.py = tam liste).

## Tuzaklar (her biri canlıda yaşandı — tekrarı yasak)

1. **Reanimated Easing**: `Easing`/`Animated` ASLA 'react-native'ten import
   edilmez → worklet crash. eslint no-restricted-imports kuralı kaldırılamaz.
   count-up-text.tsx bilerek saf JS.
2. **plans.id TEXT'tir, uuid değil** — FK yazarken `plan_id text references
   public.plans(id)`. (chat_threads migration'ında yaşandı, 42804.)
3. **Gemini 3'te thinking_budget=0 GEÇERSİZ** → client thinking_level="low"
   kullanır, fallback'te config yeniden kurulur (gemini_client._build_config).
4. **chat_threads degrade modu**: thread işlemleri try/except'lidir; thread
   hatası sohbeti/plan geçişini DÜŞÜREMEZ. Bu zırhı kaldırma (demo faciası).
5. **Migration'lar otomatik uygulanmaz**: her yeni tablo/kolon
   RUN_IN_SUPABASE_SQL_EDITOR.sql'e eklenir ve Şahin SQL Editor'da çalıştırır.
6. **Railway cron**: cron servisi Config-as-code `/railway.cron.toml` kullanır;
   yanlışsa uvicorn açılır ve 5 dk'da SIGKILL = "Deploy Crashed" spam. Tanı:
   docs/RAILWAY_SORUN_GIDERME.md.
7. **localStorage yok** (Expo) → AsyncStorage/SecureStore.
8. **Sırlar yalnız .env**; .env commit edilmez; .env.example placeholder taşır.
9. **Kullanıcı mesajı system rolüne karışmaz**; RAG içeriği etiketli CONTEXT
   bloğunda (injection önlemi). /chat sırası: SYSTEM → CONTEXT → USER, tek yer:
   core/prompt_builder.py.
10. **Railway deploy (2026-09-10)**: GitHub push canlıda deploy TETİKLEMİYOR
   (branch tetikleyicisi kapalı). `api` rootDirectory=`niyetsen-backend` →
   `railway up` REPO KÖKÜNDEN, ID flag'leriyle (`--project … --environment …
   --service api --ci`); `niyetsen-backend/` içinden çalıştırılırsa yol ikiye
   katlanır, build başlamadan FAILED. `cron` ise `niyetsen-backend/` içinden
   `railway up --service cron --ci` (configFile `/railway.cron.toml` orada).
   Doğrulama: `/health` 200 + yeni uçlar JWT'siz 401 + cron log "exit 0".
   **Production'da bekleyen staged patch (d259d2e7…) api/cron/MySQL'in TÜM
   env'lerini ve api domain'ini siler — `accept-deploy` ASLA; dashboard'dan
   discard edilmeli.**
11. **zsh'ta `path` değişkeni PATH'e bağlıdır** — script'te `path=…` yazma
   (komutlar "command not found" olur); `route`/`p` kullan.

## Dosya haritası (nereye bakılır)

- Kurallar: `CLAUDE.md` → yol haritası: `NIYETSEN_MASTER_PLAN.md` (§1 kilitli
  kararlar, §2 veri modeli) → aktif faz: `docs/FAZ8_LANSMAN.md`.
- Backend beyin: `app/services/intent_service.py` (sohbet), `plan_service.py`
  (plan), `proof_service.py` (kanıt), `fortune_service.py` (fal),
  `recap_service.py` (rapor), `scoring_service.py` (puan/rank).
- Promptlar TEK yerde: `app/core/prompts.py` (SYSTEM + FORTUNE + PROOF).
- UI: `mobile/src/constants/theme.ts` (İlkbahar token'ları — hex hardcode yasak),
  `zodiac.ts` (burç), plan: `docs/UI_V3_ILKBAHAR.md`.
- RAG bilgi tabanı: `niyetsen-backend/knowledge/` (Railway imajına COPY'lenir).

## Son UI kilitleri (2026-07-29 → 08-02, mobil)

- Tab ikonları: VectorIcon + MaterialCommunityIcons (SF web'de çizilmez).
- Sohbet: inverted FlatList; Niyetsen logo avatar; doodle wallpaper;
  web Alert → `lib/web-alert.ts`.
- Profil: soft dark Switch (`AppearanceProvider` + AsyncStorage).
- Planım: ImageScrim hafifletildi (silik örtü gitti).
- Font: Fraunces + Manrope — Figma'da Inter kullanma.

## Supabase / Play notları (2026-08-02)

- SQL Editor: yalnız VERIFY
  (`supabase/migrations/RUN_IN_SUPABASE_SQL_EDITOR.sql`). DDL yığını yapıştırma.
- Play Console Sahinology: kimlik onay aşamasında — SS güvenlik sızıntısı değil.
- Supabase Pro yükseltme hedefi: 2026-08-05.

## Çalışma protokolü

- Test: backend `pytest -q` (~184 yeşil — düşürme); mobil
  `npx tsc --noEmit` 0 hata + Expo Go gerçek cihaz.
- Commit: `faz8: <konu>` / `faz8-ui: <konu>` küçük/tek konulu.
  mobile/ AYRI git deposudur (`Niyetsen-mobile`).
- Kararsızlıkta: MASTER_PLAN §1 → bu dosya → playstore-cikis-hafiza →
  yoksa Şahin'e sor. UYDURMA.

## 2026-09-10 anlık (plan etkinlikleri + plan-içi ajan)

- Backend `c29014e`: `plan_events` / `plan_event_occurrences`, `chat_threads.kind`;
  uçlar `POST/GET /plan/{id}/events`, `DELETE /plan/events/{id}`,
  `POST /plan/events/{occ}/complete` (409 çift dokunuş), `POST /plan/{id}/chat`,
  `GET /plan/{id}/chat/history`, `/tasks/daily.events`. Etkinlik = fotosuz
  "Yaptım" +50, kaçırınca ceza YOK, gün kapanışında zinciri korur. Ajan araç
  listesi `PLAN_AGENT_TOOL_DECLARATIONS` (etkinlik_olustur yalnız orada);
  araç yanıt ÜRETİLMEDEN önce sunucuda çalışır. pytest 341.
- Mobil `4b71bc2..cfb94cf`: klavye telafisi ölçüme dayalı tek katman
  (`KeyboardAwareView` + `useKeyboardLift`; Dimensions tahmini YASAK — Android
  edge-to-edge'de pencere küçülmez), Bugün etkinlik kartı, Planım
  `PlanEventsSection` (+ editör, asistan sheet). i18n `events` bloğu 6 dil.
- Railway api `4d1f12f0` + cron `4ecacf51` canlı (bkz. Tuzak #10).

## 2026-09-06 anlık (API 1.1.1)

- Kapalı test allowlist yalnız `CLOSED_TEST_EMAILS` env; boş = kısa devre yok
  (kod fallback'i kaldırıldı). Railway `api` değişkeni dolu tutulmalı.
- JWT allowlist yalnız imzalı `email` claim; `user_metadata.email` yok sayılır.
- İkinci plan kapısı `info.status == "active"` (dev/tester DB `free` kalsa da).
- Mobil 1.1.1: klavye/KeyboardAwareView, İlkbahar paywall, lig rumuz opt-in,
  Bugün `boot-cache` var. Grok sprintindeki bölgesel lig / Realtime / “sınırsız
  AI rehberi” paywall **YAPILMAZ** (sohbet ücretsiz; lig v3 değil).
- Ekran gerçeği: 180g plan + bugün görev yok = `needs_extension`; uzatma
  Gemini bitene kadar tam ekran spinner olmamalı (kart + “uzatılıyor”).
- Paywall “Abonelik bulunamadı” = RC Customer Center boş ürün; İlkbahar
  `paywall.tsx` ayrı. Play ürün bağlanınca fiyat gelir.
